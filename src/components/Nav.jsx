import React from 'react';

class Nav extends React.Component {
  render() {
    return (
      <nav>
        <a href="#">Home</a>
        <a href="#">Login</a>
        <a href="#">Profile</a>
      </nav>
    );
  }
}

export default Nav;
